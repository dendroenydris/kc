# ── Layer-1 dataset building ──────────────────────────────────────────────────
requests>=2.31.0
tqdm>=4.66.0
PyYAML>=6.0.1
python-dateutil>=2.9.0.post0
mwparserfromhell>=0.6.6

# ── TATM experiments (Phase 2) ────────────────────────────────────────────────

# Core ML
torch>=2.1.0
transformers==4.47.1          # pinned: TL 2.x weight conversion breaks on transformers>=5.0
accelerate>=0.26.0          # required by transformers for device_map / MPS offload
huggingface-hub>=0.20.0     # model downloads

# TransformerLens + its required deps (not auto-pulled on all platforms)
transformer-lens>=2.0.0
einops>=0.7.0               # tensor manipulation used throughout TL
jaxtyping>=0.2.28           # type annotations in TL
typeguard>=4.0.0            # runtime type checking used by jaxtyping
fancy-einsum>=0.0.3         # TL internal einsum wrapper

# Probing & stats
scikit-learn>=1.3.0
scipy>=1.11.0

# Numerics — pin <2.0 to stay compatible with TransformerLens internals
numpy>=1.24.0,<2.0.0
